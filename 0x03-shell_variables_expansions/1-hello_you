#!/bin/bash
#Create a script that prints hello user, where user is the current Linux user.
user=$(whoami) # Get the current Linux user using the whoami command
echo "Hello $user" # Print hello user using the echo command
